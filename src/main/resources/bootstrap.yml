
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
  client:
    service-url:
      defaultZone: http://eureka:eureka@172.31.224.243:9902/eureka/

spring:
  application:
      name: upload-service
  cloud:
    config:
      profile: test
      label: master
      uri: ${SPRING_CONFIG_URI:http://172.31.224.243:9901}
  zipkin:
      base-url: http://47.100.47.200:9949/