
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
  client:
    service-url:
      defaultZone: http://eureka:eureka@192.168.1.11:9902/eureka/

spring:
  application:
      name: upload-service
  cloud:
    config:
      profile: dev
      label: master
      uri: ${SPRING_CONFIG_URI:http://192.168.1.11:9901}
